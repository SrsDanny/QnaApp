<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.aut.mobsoft.lab.mobsoft">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".MobSoftApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="DATABASE"
            android:value="mobsoftlab.db"/>
        <meta-data
            android:name="VERSION"
            android:value="1"/>
        <meta-data
            android:name="QUERY_LOG"
            android:value="true"/>
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="hu.bme.aut.mobsoft.lab.mobsoft.model"/>

        <activity android:name=".ui.questions.list.QuestionsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.questions.create.CreateNewQuestionActivity" />
        <activity android:name=".ui.answers.list.AnswersActivity" />
        <activity android:name=".ui.answers.create.CreateNewAnswerActivity" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="08dd0826b8c6b4799c8f52e3e700d2688f03f29d" />
    </application>
</manifest>